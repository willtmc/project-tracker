/**
 * Project status constants
 * 
 * This file defines the standard project status values used throughout the application.
 * Always use these constants instead of string literals to ensure consistency.
 */
const PROJECT_STATUS = {
  ACTIVE: 'active',
  WAITING: 'waiting',
  SOMEDAY: 'someday',
  ARCHIVE: 'archive',
};

module.exports = PROJECT_STATUS;
